# Minimal Conductor SDK config used by Codex MCP
# Note: agent_token is injected via CONDUCTOR_AGENT_TOKEN env in codex.mcp.json

backend_url: "http://localhost:4000"
websocket_url: "ws://localhost:4000/ws/agent"

projects:
  - path: "/Users/bytedance/ws/conductor"
    name: "Local Workspace"

